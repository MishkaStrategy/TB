name: Runner dependency audit

on:
  push:
    branches:
      - agent/runner-label-policy
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    uses: MishkaStrategy/ci-workflows/.github/workflows/runner-dependency-audit.yml@agent/runner-label-policy
