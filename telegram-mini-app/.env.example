# Local development: enable mock mode explicitly.
VITE_MOCK_MODE=true

# Optional API override. Production normally leaves this empty so /api is
# requested from the same HTTPS origin that serves the Mini App frontend.
VITE_API_BASE_URL=
